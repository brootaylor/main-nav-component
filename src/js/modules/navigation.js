// The navigation element
const nav = document.querySelector('.navigation');
// target the navigation button
const navButton = document.querySelector('.navigation__button');
// target the navigation list
const navList = document.querySelector('.navigation__list');
// target the navigation list items
const navItems = document.querySelectorAll('.navigation__item');
// target the navigation links
const navLinks = document.querySelectorAll('.navigation__link');

// A random function for now...
const navOpen = () => {
  console.log('The navigation is open');
};

// More to follow at some stage...

export default navOpen;
